<script setup lang="ts">
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

import {
  Sparkle,
  Tag,
  Paintbrush,
  Blocks,
  LineChart,
  Wallet,
} from "lucide-vue-next";

interface BenefitsProps {
  icon: string;
  title: string;
  description: string;
}

const benefitList: BenefitsProps[] = [
  {
    icon: "blocks",
    title: "Financial Control",
    description:
      "80% of Indian households struggle with tracking finances. Kuber provides a unified view, reducing manual tracking time by 60%.",
  },
  {
    icon: "lineChart",
    title: "Achieve Financial Goals",
    description:
      "70% of Indian millennials exceed monthly budgets. Kuber’s AI-powered tools help users save up to 15% more by adjusting budgets automatically.",
  },
  {
    icon: "wallet",
    title: "Simplified Budgeting",
    description:
      "Only 30% of Indians have a clear plan for their goals. Kuber’s goal-setting tools help users achieve them 25% faster.",
  },
  {
    icon: "sparkle",
    title: "MSMEs",
    description:
      "50% of MSMEs face cash flow issues. Kuber’s predictive insights reduce monthly cash flow uncertainty by 40%.",
  },
];

const iconMap: Record<
  string,
  | typeof Sparkle
  | typeof Tag
  | typeof Paintbrush
  | typeof Blocks
  | typeof LineChart
  | typeof Wallet
> = {
  sparkle: Sparkle,
  tag: Tag,
  paintbrush: Paintbrush,
  blocks: Blocks,
  lineChart: LineChart,
  wallet: Wallet,
};
</script>

<template>
  <section
    id="benefits"
    class="container py-24 sm:py-32"
  >
    <div class="grid lg:grid-cols-2 place-items-center lg:gap-24">
      <div>
        <h2 class="text-lg text-primary mb-2 tracking-wider">Benefits</h2>

        <h2 class="text-3xl md:text-4xl font-bold mb-4">
          Don't just be aware, Take Action
        </h2>
        <p class="text-xl text-muted-foreground mb-8">
          Your Shortcut to Success
        </p>
      </div>

      <div class="grid lg:grid-cols-2 gap-4 w-full">
        <Card
          v-for="({ icon, title, description }, index) in benefitList"
          :key="title"
          class="bg-muted/50 dark:bg-card hover:bg-background dark:hover:bg-background transition-all delay-75 group/number"
        >
          <CardHeader>
            <div class="flex justify-between">
              <component
                class="size-8 mb-6 text-primary"
                :is="iconMap[icon]"
              />

              <span
                class="text-5xl text-muted-foreground/15 font-medium transition-all delay-75 group-hover/number:text-muted-foreground/30"
                >0{{ index + 1 }}</span
              >
            </div>

            <CardTitle>{{ title }}</CardTitle>
          </CardHeader>

          <CardContent class="text-muted-foreground">
            {{ description }}
          </CardContent>
        </Card>
      </div>
    </div>
  </section>
</template>
